CC=g++	

IDLE: *.cpp src/*
	$(CC) ./idle.cpp	 -o idle	 -std=c++11 -w -g
Xscript: *.cpp src/*
	$(CC) ./script.cpp	 -o script	 -std=c++11 -w -g

clean:
	rm -rf main.exe idle.exe idle main

all: IDLE Xscript

debug:all
	gdb ./idle

run: all
	./idle
	
